Method Chaining:

Method chaining allows you to chain multiple array methods together in a single expression.
Since map(), filter(), and reduce() all return new arrays or values, you can combine them to perform multiple operations in a single line.



const numbers = [1, 2, 3, 4, 5, 6];

// Use map, filter, and reduce in a chain
const result = numbers
    .filter(num => num % 2 === 0)    // Filter even numbers
    .map(num => num * num)            // Square each even number
    .reduce((acc, num) => acc + num, 0); // Sum all squared values

console.log(result); // 56 (4 + 16 + 36)


3) Frequency Creation in Object Using Reduce

